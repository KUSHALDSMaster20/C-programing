#include<stdio.h>
 
int main(){
    /*in this case ther one float value and int
     value is ther so in the case we use float data type not int 
     bicose it return the float value not int */
     
    printf("%f \n ",2.0*2);
    return 0;
}